version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable  # o cualquier imagen básica con docker y docker-compose instalados
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Run docker-compose test
          command: |
            docker-compose -f docker-compose-test.yml up --abort-on-container-exit --build

workflows:
  version: 2
  test_workflow:
    jobs:
      - test